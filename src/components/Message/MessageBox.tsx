import React, { FC } from "react";

interface MessageBoxProps {
  children?: React.ReactNode;
}
const MessageBox: FC<MessageBoxProps> = ({ children }) => {
  return <div></div>;
};

export default MessageBox;
